import { View, Text, Image as RNImage } from "react-native";
import React from "react";

const Image = () => {
    return (
        <View>
            <RNImage />
        </View>
    );
};

export default Image;
